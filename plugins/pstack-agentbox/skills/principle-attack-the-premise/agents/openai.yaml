interface:
  display_name: "Principle Attack the Premise"
  short_description: "Question a shared premise after repeated fixes fail the same gate."
  default_prompt: "Use $principle-attack-the-premise for this repeated-failure diagnosis."
policy:
  allow_implicit_invocation: false
