# GCP coordinates of your agent box, used by gcloud-ssh-box.sh — copy to
# ~/.config/agentbox/box.env on the machine you connect FROM.
# Untracked on purpose: never commit the real values.
#AGENTBOX_VM=vm-yourname
#AGENTBOX_ZONE=some-zone-a
#AGENTBOX_PROJECT=your-gcp-project
