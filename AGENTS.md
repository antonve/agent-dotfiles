# Working on this repository

- Never hardcode machine or organization specific information or credentials.
  This repository is meant to be organization agnostic. Such values belong in
  untracked files (seeded from `.example` files), never in tracked content or
  commit messages.
